# v0.1 评审意见全文（供交叉验证）

> 日期：2026-09-22
> 评审者：**Claude（Opus 5）**
> 被评审对象：[docs/_original-v0.1/](docs/_original-v0.1/) 的五份文件（由另一模型撰写）
> 处置结果：[REVIEW-CHANGES.md](REVIEW-CHANGES.md)（35 条改动对照表）
> 本文用途：把评审的**论证过程**写下来，而不只是结论，以便第三方逐条核对、反驳或推翻。

---

## 0. 给交叉验证者的指引

请不要只判断「这些意见听起来有道理吗」。有道理的评审意见很容易写，也很容易错。请针对每一条做三件事：

1. **核对证据**：每条意见都给出了 v0.1 的具体位置。打开 [docs/_original-v0.1/](docs/_original-v0.1/) 确认我引述的原文没有被曲解。我可能读错了原意。
2. **攻击论证而非结论**：每条意见下有「**反驳这条需要什么**」一栏，写明了什么事实能推翻我。请优先检查这些。
3. **检查处置是否过度**：评审的常见失败不是漏判，而是**改得太多**。v0.2 删掉了 v0.1 的一些内容（供应链证明、指标体系、多 Agent、插件体系、四阶段用户演进）。请判断其中哪些删错了。

需要重点怀疑的三个方向（我自己最没信心的地方，详见 §7）：

- 「审查带宽是小团队真实瓶颈」这个中心论点，**我没有外部数据支撑**，它是推理而非证据；
- v0.2 把团队共享服务提到 MVP，这**显著增加了六个月的工作量**，可能已经做不完；
- 三条价值标准中的具体数字（−30%、≥70%、<50%）是我定的，**没有基线依据**。

**已知的评审者偏差**：v0.2 是我改写的。我对 v0.1 的批评和我自己的方案之间不是独立关系——我倾向于把问题定义成我的方案能解决的样子。请把这一点当作系统性偏差来校正，特别是 §3.1 和 §4.2。

---

## 1. 评审是分两轮做的，结论不同

这件事必须先说清楚，否则会看到自相矛盾的意见。

| | 第一轮 | 第二轮 |
| --- | --- | --- |
| 我当时假设的定位 | 按 v0.1 原文：**个人开发者** | 作者澄清后：**3–8 人团队 · 全周期** |
| 主要结论 | 价值主张与用户画像互相否定；领域模型严重过度建模，建议删 9 个对象 | 定位问题消失；**对象模型基本成立，删对象的建议是错的**；真正缺的是三类语义 |

**第二轮推翻了第一轮的一个主要结论。** 详见 §5「我撤回的意见」。如果你在别处看到「建议删掉 Actor / Capability / Work Graph」之类的说法，那是第一轮的，已作废。

这也说明一件事：v0.1 最大的问题不是任何单条缺陷，而是**定位没写准，导致所有下游判断都无法评估**。同一份领域模型，在个人定位下是过度建模，在团队定位下是基本合理。

---

## 2. 我认为 v0.1 做对了什么

先说这个，因为 v0.2 保留了它的大部分骨架，交叉验证时不该把它当成一份需要重写的文档。

1. **领域对象的切分是专业的。** Intent / Context / Run / Evaluation / Evidence / Decision 这六个聚合的边界划得准，职责不重叠。特别是 **Run 是执行事实、Evidence 是交付依据**这两条区分，是很多同类设计想不清楚的地方。
2. **不可变性与版本化的直觉正确。** Intent、Policy、Evaluation、Agent Profile 都要版本化，Run 固定引用某个版本——这条约束在 agentic 场景下是必需的，v0.1 一开始就写对了。
3. **拒绝重造格式。** SARIF / JUnit / Coverage 直接 Adopt，不发明替代格式。这是整份选型里价值密度最高的判断。
4. **MCP 只放在 Agent/Tool 边界**，不把内部领域 API 全改成 MCP。这个分寸感是对的。
5. **「显式的未知」原则。** 未评估、不适用必须是显式值，不允许用删除关联来掩盖。这条我原样保留，它比听起来重要。
6. **Adopt → Integrate → Extend → Build 的优先级**，以及引入依赖前的十问清单。

这六点我一条都没动。**v0.2 与 v0.1 的分歧集中在取舍与定位，不在建模能力。**

---

## 3. 第一轮：四个致命问题

### 3.1 P0-1 价值主张与用户画像互相否定

- **位置**：v0.1 VISION §7（四阶段用户演进，起点是「AI 增强型个人开发者」）对比 CHARTER §4 的目标（审批、证据、治理）。
- **断言**：个人开发者不需要审批，也不需要向自己证明变更可信。v0.1 把核心价值建立在「可审计的证据链 + 人工审批」上，而把首要用户写成单人。这两者不能同时成立：单人场景下，审批是自己批自己，证据是给自己看的——而人对自己刚写的东西不会真的去审。
- **为什么这是 P0**：它不是某一章的缺陷。用户画像决定了 MVP 里每一个功能是否必要。定位错了，路线图上每一项的优先级都无法判断。
- **反驳这条需要什么**：举出个人开发者真实愿意为「向自己证明」付出成本的证据。我能想到的最强反例是「三个月后的自己是另一个人」——代码考古时确实想知道当初为什么这么改。如果这个动机足够强，P0-1 就只是 P1。
- **处置**：定位改为 3–8 人团队；个人降为**安装单位**而非独立产品阶段（README 采用路径）。这是个折中：保留单人低门槛入口，但价值判定标准以团队为准。

### 3.2 P0-6 强制机制被假设掉了

- **位置**：v0.1 LANDSCAPE 能力版图把执行隔离写成「Git worktree、Docker」，§14 又把「Docker 是否默认启用」列为待定；同时 CHARTER 与 VISION 多处声称能「阻止」越界操作。
- **断言**：Git worktree 只隔离文件，不隔离网络、进程和凭证。**没有容器边界，所有策略都只能是检测性的**（事后发现），不可能是预防性的（事前阻止）。v0.1 一边把容器列为可选，一边声称能阻止——这个声明没有机制支撑。
- **更严重的推论（P0-6 的第二半）**：v0.1 DOMAIN §9.1 把 Policy 和 Agent Profile 存在 Git 里，而 Agent 有仓库写权限。**Agent 可以修改约束自己的规则。** 这是一条完整的提权路径，v0.1 未处理。
- **反驳这条需要什么**：说明在无容器条件下如何阻止一次 `curl` 外发，或如何阻止 Agent 编辑 `.ainative/policies/`。如果有办法，容器就不必是必需的。
- **处置**：容器改为**必需**；无容器模式保留为显式降级，且此时证据中所有策略必须标注为检测性；`.ainative/policies|agents|evaluations/` 对 Agent 只读，生效版本由服务端记录哈希（DOMAIN §9.1.1）。

### 3.3 P0-4 「证据」的差异化没有被论证

- **位置**：v0.1 VISION §2.5 列举相比现有 PR + CI 的增量。
- **断言**：它列的大部分增量，今天的 GitHub PR + CI 已经有了——diff、测试结果、覆盖率、静态分析、审批记录、CI 日志，全都有，且已经在团队的肌肉记忆里。v0.1 没有回答「**为什么我要换掉一个已经能用的东西**」。
- **我认为真实的增量只有两项**（其余全是既有能力的重新包装）：
  1. **Context Manifest**：这次改动是基于哪些信息做的。PR 里没有这个信息，而它恰恰是审查 Agent 产出时最想知道的——Agent 是不是根本没看那个关键文件。
  2. **验收标准 ↔ 评估项的映射，且标注哪些证明是本次新造的**。PR 里有测试结果，但没有「这条验收标准由哪个测试覆盖」，更没有「这个测试是 Agent 刚写的」。
- **由此派生出一条 v0.1 完全没有的约束**：Agent 同时编写实现和证明它正确的测试，形式上完全合规，实质上自己判卷。作者本人可能凭直觉察觉，接手审查的同事没有这个直觉。
- **反驳这条需要什么**：指出现有工具链中已经有 Context Manifest 或 AC↔测试映射的产品化实现（我的检索里没有，但这是可证伪的），或论证还有第三、第四项真实增量。
- **处置**：VISION §2.5 老实收缩到这两项；DOMAIN §5.6 新增 `provenance` 字段与不变量「本次运行新造的测试不得作为 critical 验收标准的唯一证据」。

### 3.4 P0-5 没有任何可被证伪的断言

- **位置**：v0.1 VISION §4.2 的北极星指标公式。
- **断言**：那个公式不可计算（分子分母都没有可采集的定义）。整份 v0.1 里，**没有一个数字能在六个月后判定项目成功或失败**。所有成功标准都是能力标准（「支持 X」「实现 Y」），做完了也不知道有没有用。
- **为什么这是 P0**：一个不能被证伪的产品方案，会一直做下去。这是这类项目最常见的死法——不是失败，是永远不结束。
- **反驳这条需要什么**：从 v0.1 原文中找出任何一条有阈值、有采集方式、失败时能触发转向的断言。
- **处置**：换成三条可证伪标准（CHARTER §8）：审查人时 −30%；证据展开率 ≥70%，**<50% 即判定核心假设被证伪**；有机扩散 ≥2 人。第二条是刻意设计成产品自己能测的——`EvidenceOpened` 与 `DecisionRecorded` 的配对（DOMAIN §7）。

---

## 4. 第二轮：小团队 · 全周期定位下的新问题

定位澄清后，P0-1 自动关闭，但浮出四个第一轮看不到的新 P0。

### 4.1 P0-2 「全周期」有两种读法，差一个数量级

- **断言**：「全周期管理平台」可以读成 (a) 追溯链覆盖全周期，每阶段深度能力靠集成；也可以读成 (b) 每个阶段的功能都自己做。v0.1 没有澄清，而 (b) 对 3–8 人团队是灾难——它等于要在六个月内做出需求管理 + 编码 + 测试 + CI + 发布 + 监控。
- **处置**：README 明确写死 (a)，并给了一句可引用的判据：**平台拥有脊椎，不拥有器官**。
- **反驳这条需要什么**：如果作者真的想做 (b)，那这不是缺陷而是我误解了意图——但那样六个月路线图需要整体重写，且 §7 的工作量担忧会严重恶化。

### 4.2 P0-3 真实瓶颈是审查带宽，不是治理

- **断言**：小团队没有合规部门、没有审计要求、没有人要求治理。他们的痛点是：**编码快了十几倍，审查和上线还按人的速度运行**。一个人一天能产出五个 PR，同事审不完。
- **两种失效方向**：要么审查成为瓶颈（PR 堆积），要么退化成橡皮章（点 approve 但没看）。**橡皮章是更常见的那个**，因为它让指标看起来很好——吞吐正常、审批齐全、追溯链完整，只是没人真的看过。
- **由此得到的唯一尺子**：每个被接受的变更所消耗的审查人时。任何不降低这个数的功能都不进第一阶段。
- **v0.1 的相应缺陷**：审查被折叠进 Approval（一个 Decision 类型），于是在设计中消失了。审查是有成本、有承载者、会排队、会退化的独立环节。
- **反驳这条需要什么**：这是我**最没有外部证据支撑**的一条，是推理。请优先攻击它。可能的反驳：(a) 小团队的真实瓶颈其实是 Agent 产出质量太差，根本到不了审查环节；(b) 小团队压根不审，直接合并，所以不存在审查成本问题——如果 (b) 为真，整个 v0.2 的价值主张都要重做。
- **处置**：新增 Review 有界上下文（DOMAIN §2.7）与 Review Assignment 对象（§5.8，含 `evidence_opened_at` 与 `time_spent_seconds`，直接用于观测橡皮章）；原则表首位改为 Review Bandwidth before Governance；LANDSCAPE §11.7 新增 Review Bandwidth Model 为自研差异化核心。

### 4.3 P0-10 两个权威来源

- **断言**：团队已经在用 Issue Tracker。v0.1 的 Intent 与 Issue 字段高度重叠（标题、目标、负责人、状态、范围），但没有规定谁是权威。结果必然是**两个「我们在做什么」的来源，且很快不一致**。这类工具死于此的案例极多——不是功能不够，是团队发现要维护两份。
- **处置**：Intent 明确为 Issue 的**结构化附件**；DOMAIN §5.2 给出字段所有权表（标题/状态/负责人 → 外部权威；验收标准/约束/风险等级 → 本地权威）；同步失败必须是显式状态；外部对象消失时 Intent 进入 `Orphaned` 而不是静默消失。
- **反驳这条需要什么**：论证小团队愿意把 Intent 当作唯一来源、放弃 Issue Tracker。我认为不可能，但如果目标团队本来就不用 Tracker，这条降级为 P2。

### 4.4 P0-8 / P0-9 单机架构失效：并发与身份

- **断言**：v0.1 的架构是「单机 CLI + 本地 SQLite + 本地身份字符串」。团队定位下这套有两个硬缺口：
  - **并发**（P0-8）：多人同时跑 Agent 是常态。v0.1 没有任何并发语义——同一分支被两个 Run 占用、路径重叠、两个 Intent 的约束互相矛盾、基线已前进，全部未定义。**并发冲突是领域语义，不是实现细节**，因为仲裁结果需要人来决定合并顺序。
  - **身份**（P0-9）：审批必须具名，而**本地配置文件里写的身份字符串不能作为审批凭据**——谁都能改。v0.1 有 Actor 对象但没有身份来源。
- **推论（这一条对小团队最危险）**：团队共享服务必须进 MVP，但 3–8 人团队**没有专职平台工程师**。任何需要他们额外运维一个进程或数据库的东西都会被弃用。
- **处置**：新增 Resource Claim 与并发仲裁表（DOMAIN §5.9）；Actor 绑定 GitHub 身份、Agent 不得持有 approve 能力、默认禁止自审（§5.10）；LANDSCAPE §1 新增两条否决条件（运维成本一票否决、必须能被单命令带起）；NFR 新增单命令部署与零迁移升级（CHARTER §7.2）。
- **反驳这条需要什么**：说明如何在纯单机、无共享服务的条件下让 B 审 A 的变更。我没找到办法，除了完全依赖 GitHub（这其实是一个值得认真考虑的替代方案——见 §7.2）。

### 4.5 P0-7 全周期的后半段没有建模

- **位置**：v0.1 DOMAIN §6.2 Run 状态机以 `Published` 结束；Release 是聚合根但**没有状态机**。
- **断言**：`Published` 把「PR 已创建」和「已发布」混为一谈。而一个全部产出就是 PR 的产品，恰恰最需要建模 PR 之后发生的事——CI 在 PR 上失败、审查者退回、合并、回滚。宣称「全周期」却在 PR 处停止，是定位与模型的直接矛盾。
- **处置**：Run 状态机拆开并补 `PullRequestOpen / Merged / Released / ChangesRequested / Contended / Stale / Reverted`；新增 Release 状态机（§6.3）；可追溯链闭合到 Feedback 回流（§10）。

---

## 5. 我撤回的意见（请核对我撤回得对不对）

第一轮在个人开发者假设下，我建议删掉 9 个领域对象作为过度建模：**Work Graph、Work Item、Capability、Tool、Actor、Release、Feedback、Risk Assessment、Context Source**。

第二轮全部撤回，理由逐条如下：

| 对象 | 撤回理由 |
| --- | --- |
| Actor | 多人身份与审批的载体，团队定位下是核心 |
| Capability | 授权的表达方式；「Agent 不得持有 approve 能力」这条不变量需要它 |
| Risk Assessment | 分级自治的输入；没有它只能全量人审，而全量人审必然退化为橡皮章 |
| Work Graph / Work Item | 任务分配给谁、依赖谁，团队场景的基本需求 |
| Release / Feedback | 「全周期」的后半段就是它们；删了就不叫全周期 |
| Context Source / Manifest 分离 | 来源的权限与敏感性是长期属性，清单是一次快照，二者生命周期不同，分开是对的 |
| Tool | 工具调用需要被策略判断和记入证据 |

**结论修正**：v0.1 的对象集合基本成立，问题不在「对象太多」，而在**缺三类语义**：并发与冲突、身份与授权、外部系统的字段所有权。见 §4.3、§4.4。

这次撤回本身值得交叉验证者注意：它说明**在定位不明的情况下做架构评审，结论会系统性偏向「删」**。如果第三方评审也倾向于削减对象，请先确认它是在哪个定位假设下判断的。

---

## 6. 其余问题（P1 / P2）

这些是文档级缺陷，不影响方向，但会在实现时变成真实成本。

| 编号 | 位置 | 问题 | 处置 |
| --- | --- | --- | --- |
| P1-1 | README / CHARTER | 没写不适用谁。不写清边界，就会为不存在的用户做功能 | 新增适用边界表（3–8 人 / 无平台工程师 / 单仓库 / 无合规 / 已用 GitHub+CI+Tracker） |
| P1-2 | CHARTER §5 | 非目标太少，六个月一定做不完 | 新增非目标：Web 审批后台、RBAC/多租户、替代 Tracker、多仓库、签名与合规报告 |
| P1-3 | VISION §8 | 自治等级表中默认等级与 MVP 目标不一致 | 统一到 L3 为默认与 MVP 目标，L4 月 5 试点 |
| P1-4 | DOMAIN Context 不变量 | 「Agent 实际收到的上下文应与 Manifest 可对账」在 CLI Agent 自主检索文件的现实下**不可达成** | 改为「必须计算差异」并新增 Context Consumption 对象。差异本身是审查信息——Agent 读了未声明的文件，往往正是该看一眼的地方 |
| P1-5 | DOMAIN §6 | Release 是聚合根却无状态机 | 新增 §6.3 |
| P1-6 | DOMAIN ID 方案 | `INT-0001` 顺序号存放在 Git 中，两个分支各建一个 Intent 必然撞号；且本地 ID 就是文件名，无法靠导出时再生成全局 ID 补救 | 改 ULID 主键 + 可读别名，别名不承担唯一性职责 |
| P1-7 | LANDSCAPE §5.1 / §6.1 | Temporal 与 OPA 都写「后续再评估」，没有决策点。而团队服务进 MVP 后，durable workflow 的理由明显变强（Run 状态机现在有 14 个状态和跨天挂起点） | 评估时点落到月 3 / 月 4，并写明判据 |
| P2-1 | DOMAIN §4 | `Feedback * ─── * EvaluationDefinition` 表达的其实是「这次事故派生出那条回归测试」，是派生边不是多对多 | 改 `derived_from` |
| P2-2 | DOMAIN §5.4 | Attempt 在正文被提及，但不在对象表和 ID 表里 | 补为一等对象 |
| P2-3 | DOMAIN | 对象表写 EVL、示例写 EVD；Policy 与 Agent Profile 无 ID 前缀 | 统一并补齐 |
| P2-4 | LANDSCAPE §14 | 九项待定选型全无截止时间 | 每项补决策时点 |
| P2-5 | CHARTER Agent Profile | 成本限额只写数字，无货币单位、无计量周期、无「无法计量时怎么办」 | 补 `amount/currency/per/source`，不可计量时必须显式为 `unavailable` |

---

## 7. 我自己最没信心的三条（建议优先交叉验证）

### 7.1 「审查带宽」是推理，不是证据

v0.2 的全部结构都挂在这一个论点上：小团队的瓶颈是审查而非治理。**我没有外部数据。** 它来自一个链条：AI 让编码提速 → 产出量上升 → 审查是人工环节且未提速 → 成为瓶颈。每一环都合理，合起来仍可能是错的。

最可能的反驳：**小团队根本不审，直接合并。** 如果这是真相，那么 v0.2 不是在解决他们的痛点，而是在推销一个他们主动放弃的环节——这比 v0.1 的定位错误更严重，因为 v0.1 至少没有把全部赌注压在一个点上。

验证成本很低：找 3 个目标团队，问他们上周有多少 PR 是在没有真正阅读的情况下被合并的。这件事应该在写任何代码之前做完。

### 7.2 团队服务进 MVP 可能让六个月做不完

我把团队共享服务从「以后」提到了月 3，理由是 P0-9（身份不可自证）。但这带来了 OAuth、并发控制、状态恢复、部署与升级——对一个人或很少人的项目，这可能吃掉一半以上的预算。

**我没有认真评估的替代方案**：完全寄生在 GitHub 上——身份用 GitHub、审查界面用 PR、状态存在 PR 评论和 Check 里、完全不做自己的服务端。这条路让「单命令部署」的问题消失，代价是能力受 GitHub API 限制、离线不可用、与「自托管优先」原则冲突。

我倾向于认为它不可行（并发仲裁和策略哈希校验需要一个有状态的判断者），但**这个结论我做得不够扎实**，请第三方重新评估。如果它可行，v0.2 的路线图应该整体重写。

### 7.3 三个数字是我拍的

−30%（审查人时）、≥70%（证据展开率）、<50% 即证伪。**没有基线依据。** 目前没有任何团队测过自己的审查人时，所以「降低 30%」的分母不存在。

更要紧的是：我把「证据展开率 <50% 就判定核心假设被证伪」写成了硬性条件。如果作者并不打算真的据此转向，这条应该删掉——留着是假承诺，而假承诺比没有承诺更坏，它会在半年后被用来解释为什么不算失败。

---

## 8. 我注意到但没有处理的问题

诚实列出，避免第三方以为这些已经想过了。

1. **产品命名**。"Control Plane" 对 3–8 人团队意味着"重"，与低门槛定位冲突。改名影响所有文档，我没有单方面改。
2. **审查者分配算法**。Review Assignment 对象定义了，但「该派给谁」（负载 / 领域熟悉度 / 风险匹配）只列为后续议题。小团队里可能根本不需要算法，人自己就知道——但也可能这正是价值点。
3. **Agent 产出质量的下限**。整个方案假设 Agent 产出值得审查。如果产出质量太低，正确的产品是别的东西。我没有处理这个前置条件。
4. **Schema 演进与迁移**。文档承诺了「零迁移升级」，但没有 Schema 版本迁移规范。这是一张迟早要还的账。
5. **提示注入的完整威胁模型**。我在风险表里列了它（Issue 正文、PR 评论是不可信输入），并要求 MCP 侧每次工具调用都过 Policy 且判断不得由模型完成，但没有做完整的威胁建模。路线图月 1 列了这项。
6. **v0.1 引用的研究材料我没有核验**。`AI-Native-SDLC-研究简报.md` 与 `research/anthropic-ai-native-sdlc-brief.md` 中的数据是否支撑 v0.1 的论断，我没有逐条检查。如果第三方要验证「AI 让编码提速十几倍」这类前提，应该从这两份材料查起。

---

## 9. 如果只验证三件事

给时间有限的交叉验证者的优先级：

1. **§7.1** — 审查带宽这个中心论点是否成立。它错了，v0.2 整体重做。
2. **§7.2** — 团队服务进 MVP 是否让六个月不可交付；纯 GitHub 寄生方案是否真的不可行。
3. **§5** — 我对 9 个领域对象的撤回是否撤得太多。反方向的风险同样真实：v0.2 可能保留了太多对象，让第一个版本过重。
